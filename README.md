# CinePlus
Cine Virtual para controlar las ventas de las entradas de CinePlus

#Requerimientos
* `.NET Core 3.1`
* `dotnet`

#Instalación
```bash
$ git clone https://github.com/arnel-sanchez/CinePlus
$ cd CinePlus/
$ dotnet ef database update
$ dotnet run
```